Welcome to the new composition service the application core system of the klangfang app.

This project is based on the Spring Boot Framework and Java.

To build this project u can use:
./gradlew build